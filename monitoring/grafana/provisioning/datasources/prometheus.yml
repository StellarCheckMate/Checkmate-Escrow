# Grafana datasource provisioning — Prometheus
#
# Place this file at grafana/provisioning/datasources/prometheus.yml
# to auto-provision the Prometheus datasource on Grafana startup.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
